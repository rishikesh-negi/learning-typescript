// Strings:
const introductionMessage: string = "Hello. Welcome to TypeScript...";

// Numbers:
const typescriptRating: number = 10;

// Boolean:
const isServerOnline: boolean = true;

// null:
const noValue: null = null;

// undefined:
const notDefined: undefined = undefined;

// If a value does not match a type, TS will throw and error during compile time:
// const errorMsg: string = 123; // Error: Type 'number' is not assignable to type 'string'
