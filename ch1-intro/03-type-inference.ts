// Type Inference:
// 95-99% of the times, TypeScript can correctly auto-assign a type to a variables if we don't explicitly specify it. This is known as type inference:
const supportAiLogs = "WARNING: Prompt overload";
console.log(supportAiLogs);
